<p id="notice"><%= notice %></p>

<p>
  <strong>Index:</strong>
  <%= badge.title %> 
  <%= badge.rule %>
  <%= badge.image_url %>
</p>

<%= link_to 'Edit', edit_badge_path(@badge) %> |
<%= link_to 'Back', badges_path %>
